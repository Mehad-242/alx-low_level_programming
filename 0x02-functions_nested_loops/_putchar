#include "main.h"
#include <unistd.h>
/**
 * _putchar - writes the character c to stdout
 * @c: the charcter to print
 *
 *Return 0n success 1.
 *0n error, -1 is returned, and error is aet appropriately.
 */
int _putchar(char c)
{
	return (write(1, &c, 1));
}
